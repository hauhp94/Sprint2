import { DrectiveServiceDirective } from './drective-service.directive';

describe('DrectiveServiceDirective', () => {
  it('should create an instance', () => {
    const directive = new DrectiveServiceDirective();
    expect(directive).toBeTruthy();
  });
});
